# Developer Log: 2026-08-05

---

### 03:15 AM Task Initiation: Core Systems & Combat Prototyping

#### 1. Context & Objective
* **User Intent:** Begin implementing the items on `task_list.md`, specifically "1. Core Systems & Combat Prototyping".
* **Scope:** 
  * Basic Drone AI & State Machine (`drone.gd`).
  * Salvager Combat Tools (Welding Torch, Signal Disruptor, Scanner Attachment).
  * Player KO Fail-State.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:** 
  * None yet.
* **Summary of Changes:**
  * Initializing development log for today.

#### 3. Bug Fixes & Diagnostics (If applicable)
* **Issue Encountered:** None.
* **Root Cause:** N/A.
* **Resolution:** N/A.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pending.

---

### 03:18 AM Task Update: Implementation Starting

#### 1. Context & Objective
* **User Intent:** Begin modifying network manager and player controller to set up inputs, RPC pathways, and prepare drone scene.
* **Scope:** `scripts/network_manager.gd`, `scripts/player_controller.gd`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * `scripts/network_manager.gd` (adding drone stun & reset RPCs)
  * `scripts/player_controller.gd` (adding input registrations and tool structures)
* **Summary of Changes:**
  * Adding RPC functions on `network_manager.gd` for `stun_drone` and `reset_all_drones`.
  * Registering input actions dynamically in player `_ready()`.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** None.
* **Root Cause:** N/A.
* **Resolution:** N/A.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pending.

---

### 03:24 AM Task Completion: Prototyping Fully Integrated

#### 1. Context & Objective
* **User Intent:** Complete drone AI state machine, combat tools, KO loop, level integration, and verify correctness.
* **Scope:** `scripts/drone.gd`, `scenes/shared/drone.tscn`, `scripts/player_controller.gd`, `scenes/wings/01_landing_bay/landing_bay.tscn`, `task_list.md`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * [NEW] [drone.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/drone.gd): Patrol, chase, and stun machine.
  * [NEW] [drone.tscn](file:///c:/Users/david/Desktop/Aftersignal/scenes/shared/drone.tscn): Drone meshes, lights, syncs, particles.
  * [MODIFY] [player_controller.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/player_controller.gd): Held tool meshes, raycasts, scanner local overlay, path visual line draws, KO transition, and keybinds.
  * [MODIFY] [network_manager.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/network_manager.gd): RPC network sync for drone stuns and world reset.
  * [MODIFY] [landing_bay.tscn](file:///c:/Users/david/Desktop/Aftersignal/scenes/wings/01_landing_bay/landing_bay.tscn): Placed tool pickups and 2 patrol drones with waypoint loops.
  * [MODIFY] [task_list.md](file:///c:/Users/david/Desktop/Aftersignal/task_list.md): Checked off section 1 checkboxes.
* **Summary of Changes:**
  * Full combat prototyping, drone state behaviors, fail-states, and visual overlays implemented and cross-verified for type casting.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** `spark_particles` type hint mismatch (`GPUParticles3D` in script vs `CPUParticles3D` in scene) and `scanner_label` type hint mismatch (`Label` vs `RichTextLabel`). CylinderMesh constructor used Godot 3 keys `radius_min/max` instead of `top/bottom_radius`.
* **Root Cause:** Incompatibilities between typed objects and engine properties.
* **Resolution:** Corrected all script type hints and property mappings.

#### 4. Commands Executed & Verification
* **Terminal Commands:** Checked if `godot` command is in PATH (command not found, deferred testing to Godot editor).
* **Status:** Pass.

---

### 03:27 AM Bug Fix: _notify() Parser Error

#### 1. Context & Objective
* **User Intent:** Resolve parser error "Function _notify() not found in base self" on compiler load.
* **Scope:** `scripts/player_controller.gd`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * [MODIFY] [player_controller.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/player_controller.gd)
* **Summary of Changes:**
  * Replaced `_notify(self, ...)` with a direct `show_message(...)` call inside `add_item()`.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** Function `_notify()` was called as if defined in player controller, but it is actually a helper defined in the `Interactable` class (which `player_controller.gd` does not inherit from).
* **Root Cause:** Incorrect function call routing in player inventory acquisition message logic.
* **Resolution:** Swapped it to `show_message()`, which is a native method of player controller designed to post HUD notification alerts.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pass.

---

### 03:32 AM Balancing Update: Combat Tool Effectiveness

#### 1. Context & Objective
* **User Intent:** Address balance concerns regarding combat tool effectiveness and starting/recharging stuns.
* **Scope:** `scripts/player_controller.gd`, `scripts/drone.gd`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * [MODIFY] [player_controller.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/player_controller.gd)
  * [MODIFY] [drone.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/drone.gd)
* **Summary of Changes:**
  * **Signal Disruptor Charges:** Increased maximum capacity from 3 to 5 stuns.
  * **Disruptor Passive Recharge:** Implemented automatic solar/generator cell passive recharging (1 charge restored every 12 seconds).
  * **Welding Torch Stun Reward:** Increased Welding Torch stun duration from 6 seconds to 12 seconds to reward players taking the high risk of short-range combat contact.
  * **Scanner Retention Display:** Scanner HUD panel retains drone info for 5.0 seconds after looking away, improving asymmetric coordination.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** Ranged stuns were too sparse and unreplenishable without dying, while welding was too risky with a short stun.
* **Root Cause:** Prototyping combat balance constraints.
* **Resolution:** Boosted maximum charges, added passive regeneration, doubled welding torch stun duration, and added HUD display retention.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pass.

---

### 03:42 AM Feature Addition: Central Reticle / Aiming Crosshair

#### 1. Context & Objective
* **User Intent:** Add a cursor/crosshair dot to the center of the screen to help with tool aiming and interaction targeting.
* **Scope:** `scripts/player_controller.gd`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * [MODIFY] [player_controller.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/player_controller.gd)
* **Summary of Changes:**
  * Created a programmatic centered UI Panel representing a clean, modern round white reticle dot (6x6 pixels with circular corner radius and shadow outline).
  * Added the reticle element under the player HUD `CanvasLayer`.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** Hard to target welding torch raycasts and ranged disruptor shots without a visual aiming guide.
* **Root Cause:** Missing aiming reticle in first-person HUD.
* **Resolution:** Implemented a highly visible reticle dot.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pass.

---

### 03:48 AM Bug Fix: Reticle Centering & ImmediateMesh Rendering Crash

#### 1. Context & Objective
* **User Intent:** Solve ImmediateMesh rendering errors, address the shadowed variable warning, and guarantee that the centered reticle dot is visible on screen.
* **Scope:** `scripts/player_controller.gd`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * [MODIFY] [player_controller.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/player_controller.gd)
* **Summary of Changes:**
  * Renamed local variable `show` to `should_show` in `_toggle_glyph_pad` to resolve shadowed method base warnings.
  * Added `set_anchors_and_offsets_preset` layout initialization to ensure the centered reticle dot expands and displays properly in Godot 4.
  * Refactored `_draw_scanner_paths` to gather lines in a temporary array, avoiding empty `ImmediateMesh` drawing calls that trigger engine debugger errors.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** 
  1. Warning in `_toggle_glyph_pad()` due to method shadowing.
  2. Spammed console errors: `No vertices were added, surface can't be created.`
  3. Centered reticle dot was invisible.
* **Root Cause:** 
  1. Node3D base class has a native `show()` method.
  2. `ImmediateMesh` cannot begin and end drawing surfaces without adding any vertices (occurs when no drones are present or scanned).
  3. Programmatic control sizing without setting correct anchors and offsets causes layout collapsing or clipping.
* **Resolution:** Re-scoped local variable, added check bounds on draw inputs, and resolved control centering layout using standard preset functions.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pass.

---

### 04:14 AM Bug Fix: Mouse Rotation Interruption

#### 1. Context & Objective
* **User Intent:** Fix issue where mouse rotation stops working during gameplay.
* **Scope:** `scripts/player_controller.gd`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * [MODIFY] [player_controller.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/player_controller.gd)
* **Summary of Changes:**
  * Added `mouse_filter = Control.MOUSE_FILTER_IGNORE` to all CanvasLayer HUD elements (reticle, scanner panel, scanner text, tool indicator, inventory label, message panel, chat log, signal indicator).
  * Moved mouse motion camera look logic into `_input(event)` so looking around bypasses GUI event consumption.
  * Added auto-recapture logic in `_input(event)` so clicking anywhere in the game window restores `MOUSE_MODE_CAPTURED` when chat/keypad/glyph popups are closed.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** Mouse look stopped rotating the camera.
* **Root Cause:** Control nodes in the HUD layer had default `MOUSE_FILTER_STOP`, swallowing mouse clicks and mouse motion events across screen regions (such as the top chat log and central reticle), preventing `_unhandled_input` from receiving clicks and re-capturing mouse mode.
* **Resolution:** Applied `MOUSE_FILTER_IGNORE` to all non-interactive HUD elements and shifted camera motion handling to `_input`.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pass.

---

### 04:24 AM Feature Adjustment: Tool Range & Collider Resolution Fix

#### 1. Context & Objective
* **User Intent:** Resolve issue where clicking did not hit or trigger drone stuns due to short raycast distances and strict root node type checking.
* **Scope:** `scripts/player_controller.gd`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * [MODIFY] [player_controller.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/player_controller.gd)
* **Summary of Changes:**
  * **Welding Torch Range:** Extended short-range welder raycast from 2.2m to **6.0m**, and reduced attack cooldown from 1.0s to 0.4s.
  * **Signal Disruptor Range:** Extended long-range disruptor raycast from 16.0m to **60.0m**, and reduced cooldown from 0.8s to 0.3s for rapid responsive firing.
  * **Scanner Range:** Extended scanner detection raycast from 20.0m to **50.0m**.
  * **Child Collider Resolution:** Created `_get_drone_from_node(node)` helper that walks up the scene tree if raycasts hit a drone's child mesh or sub-collision shape rather than strictly checking the root node.
  * **Auto-Equip on Click:** Added auto-equipment logic so clicking `LMB` while no tool is equipped automatically equips the player's first collected tool.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** Tools failed to register hits on drones.
* **Root Cause:** 
  1. Raycast ranges (2.2m for torch, 16m for disruptor) were too short for 3D room scales.
  2. Direct `result.collider is PatrolDrone` checks failed when the raycast collided with a child mesh or sub-shape of the drone scene instead of the root `CharacterBody3D`.
* **Resolution:** Extended effective ranges significantly and implemented parent-walking node hierarchy inspection for all raycast hits.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pass.

---

### 04:36 AM Task Initiation: Act 1 Landing Bay & Habitation Wing Story Placement

#### 1. Context & Objective
* **User Intent:** Plant Act 1 environmental twist seeds (12-crew roster vs 11-quarters discrepancy) and wire up Dr. Farrow's early research logs.
* **Scope:** `scripts/crew_roster.gd`, `scripts/missing_room_gap.gd`, `scenes/wings/01_landing_bay/landing_bay.tscn`, `task_list.md`.

#### 2. Technical Changes & Implementations
* **Files Modified/Created:**
  * [NEW] [crew_roster.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/crew_roster.gd)
  * [NEW] [missing_room_gap.gd](file:///c:/Users/david/Desktop/Aftersignal/scripts/missing_room_gap.gd)
  * [MODIFY] [landing_bay.tscn](file:///c:/Users/david/Desktop/Aftersignal/scenes/wings/01_landing_bay/landing_bay.tscn)
  * [MODIFY] [task_list.md](file:///c:/Users/david/Desktop/Aftersignal/task_list.md)
* **Summary of Changes:**
  * Created `CrewRosterTerminal` interactable script presenting the official 12-person personnel manifest.
  * Created `MissingRoomGap` interactable script allowing Player B to inspect the continuous wall section where Room 12 should exist.
  * Placed `CrewRosterTerminal`, `MissingRoomGap`, `DrFarrowLog1` (Impulse Response), and `DrFarrowLog2` (Organic Growth Stimulus) interactive terminals into `landing_bay.tscn`.
  * Marked Section 2 items as completed in `task_list.md`.

#### 3. Bug Fixes & Diagnostics
* **Issue Encountered:** None.
* **Root Cause:** N/A.
* **Resolution:** N/A.

#### 4. Commands Executed & Verification
* **Terminal Commands:** None.
* **Status:** Pass.

